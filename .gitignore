# Private / heavy / non-site files — never publish
Новая папка/
_extract/
.playwright-mcp/
.claude/

# local backups & temp
index.original-*.html
*.zip
_*.png
verify-*.png

# secrets
*.env
*secret*
*.credentials*

# dev scratch / research — not part of live site
badge-examples/
templates/
badge-verdysh.html

# build tooling (not part of published site)
_build/

# local preview (not published)
preview-hero.html
